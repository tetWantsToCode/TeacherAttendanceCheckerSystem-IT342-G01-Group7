import React from "react";

export default function TempPage() {
	return (
		<div style={{ display: "flex", flexDirection: "column", alignItems: "center", marginTop: "40px" }}>
			<h1>Logged in!</h1>
			<p>You have reached the temporary page after login.</p>
		</div>
	);
}